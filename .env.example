# ===========================================
# OpenStore Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values
# 
# NOTE: Most configurations (payment gateways, integrations, etc.)
# are managed via the admin interface after initial setup.
# Only core infrastructure settings are needed here.

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/openstore

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# JWT secret key (minimum 32 characters, use a strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-minimum-32-chars

# ===========================================
# ENCRYPTION (Required for storing API keys)
# ===========================================
# Encryption key for sensitive data (32 characters)
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# SITE CONFIGURATION
# ===========================================
# Public site URL (used for SEO, callbacks, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ===========================================
# ENVIRONMENT
# ===========================================
NODE_ENV=development
